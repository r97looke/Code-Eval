#!/usr/bin/env python
import sys

def main():
	with open(sys.argv[1]) as f:
		for l in f:
			print l

		#build a list of all previously seen
		#list =l[0]
		#if l[1] not in list
		#list append l[1]

		#loop for next char

if __name__=="__main__":
   main()