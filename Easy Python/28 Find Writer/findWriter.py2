#!/usr/bin/env python

import sys
import fileinput

"""
findWriter
"""
def findWriter(line):
	print line


"""
Main method takes the input, strips and splits it into a string list
"""
def myfileInput(file):

	for line in fileinput.input([file]):
		findWriter(line.rstrip().split())


if __name__=="__main__":
	myfileInput(sys.argv[1])