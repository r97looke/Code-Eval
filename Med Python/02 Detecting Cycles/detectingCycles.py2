#!/usr/bin/env python

import sys
import fileinput

def file_input(line):

	numbers = set()
	for line in fileinput.input([line]):
		line = line.split()
		for x in line:
			if x not in numbers:
				numbers.add(x)
			else:
				print x
	


if __name__=="__main__":
	file_input(sys.argv[1])
